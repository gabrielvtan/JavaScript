// this will run the npm installation whenever the file is run .
import { sum, kebabCase } from 'lodash'; 
import { addTax } from './checkout';

console.log(kebabCase('Gabby isssss ssuuupppper cooooooooool'))

console.log(addTax(100, 0.15));